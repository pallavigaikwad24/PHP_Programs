<?php

    // Data Types in PHP

    // String
    $fullName = "Pallavi Gaikwad";
    echo var_dump($fullName);

    // Integer 
    $income = 16000;
    echo var_dump($income);

    // Float
    $price = 19.30;
    echo "$".$price. var_dump($price). "\n";

    // Boolean 
    $isChar = false;
    echo var_dump($isChar);

    // NULL
    $empty = NULL;
    echo var_dump($empty);

    // Array
    $colors = array("red", "blue", "green");
    echo var_dump($colors);
    echo $colors[0];

?>